{
  "framework": null,
  "buildCommand": "npm install && mkdir -p public && echo '<html></html>' > public/index.html",
  "rewrites": [
    { "source": "/api/v1/(.*)", "destination": "/api/v1?url=$1" }
  ]
}
